import { mentalHealthRequest } from './mentalHealth';
import { counselor } from './counselor';
import { chatRoom } from './chatRoom';

export const schemaTypes = [
  mentalHealthRequest,
  counselor,
  chatRoom,
];
